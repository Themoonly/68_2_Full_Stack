function findparameter(lenght, width) {
    const parameter = 2 * (lenght, width)
    return parameter;
}

let result = findparameter(5, 10);
console.log(`the parameter is: ${result}`);
console.log(`the parameter is: ${findparameter(5, 10)}`);